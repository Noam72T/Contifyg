// Hooks optimisés pour l'API - Stub pour éviter les erreurs de compilation
// Ces hooks ne sont pas utilisés dans la version actuelle

export const useEmployees = (_companyId?: string) => {
  return {
    data: null,
    loading: false,
    error: null,
    mutate: () => {}
  };
};

export const useRoles = (_companyId?: string) => {
  return {
    data: null,
    loading: false,
    error: null,
    mutate: () => {}
  };
};

export const usePartenariats = (_companyId?: string) => {
  return {
    data: null,
    loading: false,
    error: null,
    mutate: () => {}
  };
};
