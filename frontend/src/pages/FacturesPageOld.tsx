import React from 'react';
import Layout from '../components/Layout';

// Fichier stub pour remplacer FacturesPageOld.tsx qui avait des erreurs de compilation
// Ce composant n'est pas utilisé dans l'application actuelle

const FacturesPageOldStub: React.FC = () => {
  return (
    <Layout>
      <div className="p-6">
        <h1 className="text-2xl font-bold mb-4">Page Factures (Ancienne Version)</h1>
        <p className="text-muted-foreground">
          Cette page a été désactivée temporairement en raison d'erreurs de compilation.
          Utilisez la version actuelle de la page Factures.
        </p>
      </div>
    </Layout>
  );
};

export default FacturesPageOldStub;
